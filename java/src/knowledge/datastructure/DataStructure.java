package knowledge.datastructure;

import knowledge.datastructure.adv.AdvancedDS;
import knowledge.datastructure.array.Arrays;
import knowledge.datastructure.graph.Graph;
import knowledge.datastructure.hash.Hash;
import knowledge.datastructure.heap.Heap;
import knowledge.datastructure.list.LinkedList;
import knowledge.datastructure.other.MinMaxContainer;
import knowledge.datastructure.queue.Queue;
import knowledge.datastructure.stack.Stack;
import knowledge.datastructure.string.IString;
import knowledge.datastructure.tree.Tree;

/**
 * @author wujingxinit@outlook.com
 * @date 2026/07/26
 * @description 数据结构 (Data Structure) 全景索引
 * <基础数据结构>
 * @see Arrays          数组 (矩阵、区间操作)
 * @see LinkedList      链表 (反转、双向、循环链表)
 * @see Stack           栈 (单调栈、括号匹配)
 * @see Queue           队列 (单调队列、优先队列、双端队列)
 * <树与图>
 * @see Tree            树 (BST、AVL、红黑树、遍历、序列化)
 * @see Graph           图 (最短路、网络流、连通性、二分图)
 * <字符串与哈希>
 * @see IString         字符串 (KMP、AC自动机、后缀自动机SAM)
 * @see Hash            哈希表 (存在性去重、计数分组、前缀和、哈希设计)
 * <高级数据结构>
 * @see AdvancedDS      高级数据结构 (线段树、树状数组、并查集、ST表、主席树、树链剖分)
 * @see Heap            堆与优先队列 (双端堆、TopK、斐波那契堆)
 * @see MinMaxContainer 最值容器 (跳表、树状多重集合)
 */
public interface DataStructure {
}
